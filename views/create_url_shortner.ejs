<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Link Shortener</title>
    <style>
      html, body {
        margin: 0;
        padding: 0;
        min-height: 100vh;
        font-family: 'Inter', Arial, sans-serif;
        background: #f4f6fa;
        color: #23272f;
        transition: background 0.4s, color 0.3s;
        scroll-behavior: smooth;
      }
      body.dark, .dark body {
        background: #161c26 !important;
        color: #fafafa;
      }
      html, body {
        margin: 0;
        padding: 0;
        min-height: 100vh;
        font-family: 'Inter', Arial, sans-serif;
        background: #f4f6fa;
        color: #23272f;
        transition: background 0.3s, color 0.3s;
      }
      .dark html, .dark body {
        background: #161c26 !important;
        color: #fafafa;
      }
      main {
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-top: 2rem;
      }
      form {
        display: flex;
        align-items: center;
        background: #fff;
        border-radius: 1rem;
        box-shadow: 0 4px 20px #0001;
        padding: 24px 32px;
        gap: 1rem;
        margin-bottom: 2rem;
        transition: background 0.3s;
      }
      .dark form {
        background: #212848;
        box-shadow: 0 4px 20px #001c;
      }
      input[type="text"] {
        padding: 14px 18px;
        font-size: 18px;
        border-radius: 5px;
        outline: none;
        border: 1px solid #d1d5db;
        min-width: 340px;
        max-width: 440px;
        background: #f8fafc;
        color: inherit;
        transition: border 0.2s, background 0.3s;
      }
      .dark input[type="text"] {
        background: #192034;
        border: 1px solid #314e6b;
        color: #f3f6fa;
      }
      button[type="submit"] {
        padding: 14px 24px;
        border: none;
        background: linear-gradient(90deg, #2563eb 40%, #3b82f6 60%);
        color: #fff;
        font-size: 1.1rem;
        border-radius: 8px;
        cursor: pointer;
        font-weight: 700;
        letter-spacing: .03em;
        transition: background 0.2s, color 0.2s;
        box-shadow: 0 2px 8px #0001;
      }
      button[type="submit"]:hover {
        background: linear-gradient(90deg, #3b82f6 35%, #2563eb 85%);
      }
      @media (max-width: 720px) {
        form { flex-direction: column; box-shadow: none; padding: 16px 4px; }
        input[type="text"] { min-width: 180px; width: 100%; }
      }
    </style>
</head>
<body>
    <%- include('header') %>
    <main>
        <form action="/shorten" method="POST">
            <input name="link" type="text" placeholder="Paste your long link here">
            <button type="submit">Shorten</button>
        </form>
        <%- include('show_urls') %>
    </main>
</body>
</html>