<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
        }
        body{
            background-color: whitesmoke;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
      
     
        .card {
            display: flex;
            flex-direction: column;
            gap:32px;
            margin: 50px 20px;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border-radius: 1rem;
            padding: 1.5rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            overflow: hidden;
        }
        .url-box {
            margin-bottom: 1rem;
            margin-right: 1rem;
            padding: 0.75rem;
            background: #ebe1ed;
            border-radius: 0.5rem;
            word-break: break-all;
        }
        .link {
            color: #3b82f6;
            text-decoration: none;
            font-weight: 500;
        }
        .link:hover {
            color: #2563eb;
        }
        .link i {
            margin-right: 0.5rem;
        }

        .count {
            border: 1px solid rgb(147, 147, 147);
            padding: 4px;
            border-radius: 6px;
        }
        .inner-container{
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        #original_url{
            text-wrap: wrap;
            word-break: break-all;
        }
        .deleteUrl{
            background-color: rgb(255, 237, 237);
            border:  1px solid black;
            padding: 4px 8px;
            border-radius: 4px;
            cursor: pointer;
        }
       
    </style>
</head>
 <script>
        async function deleteUrl(id) {
            try {
                const response = await fetch(`/link/${id}`, {
                    method: 'DELETE'
                });
                if (response.ok) {
                    window.location.reload();
                }
            } catch (error) {
                console.error('Error deleting URL:', error);
            }
        }
 </script>
<body>
    <div class="container">
            <% urls.map((url) => { %>
                <div class="card">
                   <div class="inner-container">
                     <div>
                        <a class="url-box" target="_blank" href="<%= url.shortLink %>">
                            <i class="fas fa-link"></i>
                            <%= url.shortLink %>
                        </a>
                        <span class="count"> <%= url.clicks %> clickes</span>
                    </div>
                    <button class="deleteUrl" onclick="deleteUrl('<%= url._id %>')"><i class="fa-solid fa-trash"></i></button>  
                    </div>
                   <div>
                     <a class="link" target="_blank" href="<%= url.longLink %>">
                        <i class="fas fa-external-link-alt"></i>
                        <span id="original_url"><%= url.longLink %></span>
                    </a>
                   </div>
                </div>
            <%}) %>
        </div>
</body>
</html>